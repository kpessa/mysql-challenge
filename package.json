{
  "name": "10__SQL-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "all": "mysql -h localhost -u root -p < schema.sql && mysql -h localhost -u root -p < seed.sql &&node app.js",
    "migrate": "mysql -h localhost -u root -p < schema.sql",
    "seed": "mysql -h localhost -u root -p < seed.sql",
    "start": "node app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "console.table": "^0.10.0",
    "execsql": "0.0.3",
    "inquirer": "^8.0.0",
    "mysql2": "^2.2.5"
  }
}
